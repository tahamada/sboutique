<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <!--<li class="active"><a href="#">Accueil<span class="sr-only">(current)</span></a></li>-->
        <li><a href="{{path('homepage')}}">Accueil<span class="sr-only"></span></a></li>
        <!--<li><a href="#">Link</a></li>-->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categorie <span class="caret"></span></a>
          <ul class="dropdown-menu">
           {% for item in categorie %}
              <li><a href="/?categorie={{item.id}}">{{item.nom}}</a></li>
            {% endfor %}
          </ul>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
          <ul class="dropdown-menu">
              <li><a href="">Ajouter Article</a></li>
              <li><a href="">Gerer Categorie</a></li>
          </ul>
        </li>
        {% endif %}

      </ul>
      <div class="navbar-form navbar-right">
        <span id="btnPanier" class="glyphicon"  title="Mon panier"><img src="{{ asset('images/panier2.png') }}"><span id="nbPanier"></span></span>               
        <span id='connexion'>
          {% if is_granted('ROLE_USER') %}
            {% include 'monMenuCompte.html.twig' %}
          {% else %}
            <a href="{{path('fos_user_security_login')}}"><span class="glyphicon glyphicon-log-in" id="btnLogIn" title="Connexion"></span></a>
            <a href="{{path('fos_user_registration_register')}}"><span class="glyphicon glyphicon-pencil" id="btnLogIn" title="Inscription"></span></a>
          {% endif %} 
        </span>
      </div>
      
      <form action="/" method="GET" class="navbar-form  navbar-right">
        <div class="form-group ">
          <input type="text" name='designation' class="form-control" placeholder="rechercher un article">
          <select class="form-control" name="categorie">
            <option value="all">All</option>
           {% for item in categorie %}
                <option value="{{item.id}}">{{item.nom}}</option>
            {% endfor %}
          </select>
        </div>
        <button type="submit" name="recherche" class="btn btn-default"><span class="glyphicon glyphicon-search"  title="Rechercher"></span></button>
      </form>      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>